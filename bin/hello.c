#include <stdio.h>
#include <unistd.h>

void delay (int j)
{
    int i = 0;

    for ( i = 0 ; i <= j; i ++ ) 
    {
        ;
    }
}

/* main line */
int main ( void ) 
{
    printf ( "hello world ! \n" );

    while (1);

    return (0);
}
